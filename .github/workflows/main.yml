name: Java CI/CD Pipeline

on:
  push:
    branches: [ 5-messaging-and-cicd ] # Se activa al subir a esta rama
  pull_request:
    branches: [ 5-messaging-and-cicd ]

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Set up JDK 11
      uses: actions/setup-java@v3
      with:
        java-version: '11'
        distribution: 'temurin'
        cache: maven

    - name: Build with Maven
      run: mvn clean package -DskipTests # Compila los 3 servicios

    - name: Build Docker Images
      run: |
        docker-compose build # Verifica que los Dockerfiles funcionan